name: Python Tests

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Miniconda
        uses: conda-incubator/setup-miniconda@v2
        with:
          python-version: 3.12  # Ensure this matches the environment.yml
          auto-update-conda: true
          environment-file: environment.yml
          activate-environment: cocopaths-env

      - name: install Infrared
        run: |
           conda install conda-forge::infrared 
        working-directory: . 

      - name: Verify installation of infrared
        run: |
          conda list infrared
        working-directory: .

      - name: Run pytest
        run: |
          conda activate cocopaths-env
          pytest
        working-directory: .

      - name: Run coverage
        run: |
          source activate cocopaths-env
          coverage run -m pytest
        working-directory: .

      - name: Upload coverage reports to Codecov
        uses: codecov/codecov-action@v3
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
