name: cocopaths-env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - infrared
  - pip
  - pip:
    - flit
    - peppercornenumerator
    - pytest
    - pytest-cov
    - crnsimulator
    - coverage
    - viennarna
